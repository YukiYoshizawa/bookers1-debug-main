<h1>Editing Book</h1>

<%= form_with model: @book, local:true do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>
<% end %>

<%= link_to 'Show', books_path(@book.id) %> |
<%= link_to 'Back', books_path %>
